name: Security Scanning

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'

jobs:
  source:
    uses: Gradient-DS/security-ci/.github/workflows/security-source.yml@33d2ef167da03d3edfd6732684c17347cc957c82 # v5
    secrets: inherit
    permissions:
      contents: read
      issues: write
    with:
      python-paths: 'warren/'
      python-uv: true

  gate:
    needs: [source]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Fail unless every security job succeeded
        env:
          SOURCE: ${{ needs.source.result }}
        run: |
          echo "source=$SOURCE"
          [ "$SOURCE" = "success" ]
