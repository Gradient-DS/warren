"""Unit tests for the ``warren`` package.

Lives outside ``warren/`` (alongside ``e2e_test/`` and
``runtime_scripts/``) so the package dir stays shippable; the tree
mirrors ``warren/``'s package layout.
"""
